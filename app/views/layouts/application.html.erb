<!DOCTYPE html>
<html>
<head>
  <title>StackUnderFlow</title>
  <link rel="stylesheet" type="text/css" href=".css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "StackUnderflow", "/", :class => "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li><a href="#">Hi, <%= current_user.username.capitalize %>!</a></li>
          <li><a href="#">Ask a Question</a></li>
          <li class="active"><%= link_to "Logout", logout_path, method: :delete %></li>
        <% else %>
          <li class="active"><%= link_to "Sign Up", signup_path %></li>
          <li class="active"><%= link_to "Login", login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<%= yield %>

</body>
</html>

<!-- #if user logged in
  #have a logout button
  #ask a question
#if user not logged in
  #log in button and sign up button -->
